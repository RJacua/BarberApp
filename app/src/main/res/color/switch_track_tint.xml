<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor para o estado ligado (checked) -->
    <item android:color="@color/colorSwitchOn" android:state_checked="true" />

    <!-- Cor para o estado desligado (unchecked) -->
    <item android:color="@color/colorSwitchOff" android:state_checked="false" />
</selector>
