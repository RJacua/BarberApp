package com.example.barberapp.Schedule

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class BarberScheduleViewModel(application: Application) : AndroidViewModel(application){
}